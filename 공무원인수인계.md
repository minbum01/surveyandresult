# 인수인계 — 공무원 결과페이지 & 후기 큐레이션 (집에서 이어서)

> 작성: 결과페이지(공무원) 터미널 · 2026-06-27
> 이 문서 하나로 **공무원 result.html + 후기 검수(큐레이션) 작업**을 이어서 할 수 있게 정리.
> ⚠️ 멀티 터미널 프로젝트 — **`터미널간공유문서.md` 먼저 읽고** 충돌 피할 것.

---

## 0. 지금 어디까지 했나 (한 줄)
공무원 결과페이지(`result.html`) 1·2면 + **조합별 후기 검수 도구(`curate_confirm.html`)** 완성. 핀 1,100여 건 Supabase 라이브 반영됨. 우측패널을 막 개편함(아래 §3).

---

## 1. 내 소유 파일 (공무원 — 다른 터미널 안 건드림)
| 파일 | 역할 |
|---|---|
| `live/result.html` | 공무원 결과 리포트 (1면 페르소나/우측분석 + 2면 전략/강의) |
| `live/curate_confirm.html` | **후기 검수 도구** — 조합별 top후보 보고 확정/제외 |
| `build_curate_candidates.py` | 후보 생성 → `live/curate_candidates.json` |
| `build_result_stats.py` | 2면 강의/전략 집계 → `live/result_stats.json` |
| `sync_exclude.py` | 강사 검수목록 마크 → `live/result_exclude.json` 재도출 |
| `regen_review_md.py` | `강사교재_검수목록.md` 정규화 재생성 |
| `live/result_exclude.json`·`subject_canon.json`·`curation_manual.json`·`aliases_manual.json` | 강사/과목 큐레이션 데이터 |
| `강사교재_검수목록.md` | 강사 검수 surface |
| `후기매칭_검수가이드.md` | 각 박스 매칭로직·검수법 가이드 |

⛔ **안 건드리는 것(타 터미널)**: `curation_sync.js`·`supa.js`·`no01_폴백_매칭수.html`의 ☁코드·`vercel.json` (서버/배포 터미널) / `police_*`·`auto_curate_police.py`·`build_police_stats.py` (경찰 터미널).

---

## 2. 검수 → 배포 파이프라인
```
1) curate_confirm.html      후기 확정/제외        → localStorage(admin_curation_v2)
2) no01_폴백_매칭수.html ☁  서버 저장(로그인)    → Supabase curation(공무원 행)
3) result.html (라이브)     loadRemote로 읽어     → 답변별 핀 후기 표시
```
- dev 서버: `python -m http.server 8181` (한 곳만). URL `http://localhost:8181/live/...`
- ☁ 서버 저장 버튼: `no01_폴백_매칭수.html` 상단 파란 버튼(로그인 필요).
- 핀이 서버에 있으면 라이브에 반영. 빌드스크립트(result_stats 등)는 git push로 배포(서버 터미널 담당).

---

## 3. result.html 1면 우측패널 — 방금 개편 (2026-06-27)
| 박스 | 매칭 | 내용 |
|---|---|---|
| 같은 출발점(no.06) | Q1×Q4 | 카드 1~2장 (핀 랜덤) |
| **내 약점은 이렇게 보완했다!(no.07)** | Q8(고민)+극복서사 | **2~3 사례** (리뷰당 1개, 셔플) |
| **내 직렬 팁(no.10)** | Q3(직렬) 전공과목+공부조언 | **2~3 팁** (`SUBJECT_BY_JOB` 맵) |
- **삭제됨**: 첫 한 달 풍경(no.08), 포기 직전(no.09) — 사용자 요청(쓸 수 없는 매칭).
- 모든 단일픽 박스(no.02/07/10)는 **상위 풀에서 랜덤** → 500명 각자 다양한 후기.
- 검수 영역: no.01·06·07 (no.08/09 제거됨, no.02/10은 자동매칭).

---

## 4. 자주 쓰는 명령
```bash
python build_curate_candidates.py    # 후보 재생성 (TOPN=50, 자동제외 이중석/2023/2년이상)
python build_result_stats.py         # 2면 강의/전략 집계 재생성
python sync_exclude.py               # 강사 검수목록 마크 → exclude.json + 통계 재빌드
python regen_review_md.py            # 검수목록 정규화 재생성
```
- 검수 자동제외 기준: **이중석 언급 / 2023년 / 2년이상**(소프트, 그것밖에 없으면 유지).
- result.html 수정 후 JS 문법체크: inline `<script>` 추출 → `node --check`.

---

## 5. 남은 일 / 다음 단계
- [ ] 개편한 우측패널(no.07 약점보완·no.10 직렬팁) **로컬에서 눈으로 검수** — 내용 적절한지, 직렬별 전공팁 잘 나오는지
- [ ] no.07 약점보완은 **curate_confirm 영역=no.07** 에서 핀 검수 가능 (Q8별)
- [ ] no.10 직렬팁은 현재 **자동매칭**(검수 X) — 품질 낮으면 `SUBJECT_BY_JOB`/`ADVICE` 키워드 조이거나 검수영역 추가
- [ ] (선택) no.02 히어로쿼트 검수영역 추가
- [ ] 최종 핀 재확정(♻️ 전체 재확정) → no01 ☁ 저장 → 라이브 확인
- [ ] 2면 강의카드 강사 검수 마저(강사교재_검수목록.md ❌/✅)

---

## 6. 멀티터미널 주의
- **공유문서 먼저 읽기**(`터미널간공유문서.md`) — 서버/경찰 터미널과 충돌 방지.
- result.html은 **서버 터미널이 print-ticket/큐 코드 공동편집** 중 → print 코드 안 건드림. 내 변경(우측패널·랜덤화)은 working tree에 있음.
- `git add -A` 금지 — **내 파일만 명시적으로 add**.
- push는 원래 서버 터미널 담당이나, 사용자 지시로 이번엔 내가 내 파일들 commit·push함(아래 §7).

---

## 7. 마지막 커밋/푸시 기록
- (이 줄은 push 후 커밋해시 기입) 공무원 결과페이지 우측패널 개편 + 검수도구/큐레이션 일괄.
