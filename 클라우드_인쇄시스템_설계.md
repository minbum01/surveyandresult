# 클라우드 배포 + 실시간 인쇄 시스템 설계서

> 작성일: 2026-06-26
> 목적: 행사장 태블릿(스탠바이미) 설문 → 결과 표시 → 종이 출력까지, 클라우드 기반으로 구현
> 이 문서만 보고 사무실에서 그대로 구현 가능하도록 작성

---

## 0. 한 줄 요약

**Netlify(정적 사이트) + Supabase(큐레이션·인쇄큐·인증) + 프린트 스테이션 2대(로컬 PC+프린터)** 구조.
태블릿은 설문·결과 표시만, 인쇄는 스테이션이 클라우드 큐를 받아 처리.

---

## 1. 행사 환경 / 요구사항 (확정)

| 항목 | 내용 |
|------|------|
| 규모 | **약 500명 / 3시간** (시간당 ~167명, 동시 피크 10~20명) → 부하 낮음 |
| 단말 | **스탠바이미(LG webOS 태블릿)** — 브라우저로 설문·결과만. **프린터 직접 연결 불가** |
| 출력 | **프린트 스테이션 2대** (윈도우 PC + 레이저 프린터). 21초당 1장 수준 → 2대로 여유 |
| 인쇄 트리거 | **결과 도달 시 자동 인쇄** (탭 0회) + "다시 인쇄" 버튼 폴백 |
| 작업 분배 | 2대가 **무충돌 큐**(`FOR UPDATE SKIP LOCKED`)로 자동 분배 — 중복 출력 0 |
| 전제 | **행사장 인터넷 안정** 필수 (사이트·큐가 클라우드). 유선/전용 와이파이 권장 |

### 확정된 결정사항
1. 후기 JSON **슬림화** 진행 (첫 로딩 속도 ↑)
2. 관리자 인증 = **Supabase 이메일/비번 1개**
3. 인쇄 = **자동 등록 + SKIP LOCKED 큐 + 2대 자동분배**
4. 인쇄는 스테이션이 담당 (로컬 `print_server.py` 재활용, 태블릿은 출력 안 함)

---

## 2. 전체 아키텍처

```
┌─────────────────────────── Netlify (정적 CDN) ───────────────────────────┐
│  live/* (home·설문·결과·result.html·police_result.html·curation_sync.js)   │
│  + 후기 JSON(슬림본)  + supa.js(Supabase 클라이언트 설정)                   │
└───────────────┬──────────────────────────────────┬───────────────────────┘
                │ 읽기(anon)                         │ 인쇄등록(anon RPC)
                ▼                                    ▼
        ┌───────────────────────── Supabase ─────────────────────────┐
        │  curation 테이블   : 핀 JSON (공개 읽기 / 관리자만 쓰기)       │
        │  print_jobs 테이블 : 인쇄 작업 큐                            │
        │  stations 테이블   : 스테이션 heartbeat                      │
        │  RPC: enqueue_print / claim_next_job / complete_job / fail   │
        │  Auth: 관리자 이메일+비번                                     │
        └───────▲───────────────────────────────────▲────────────────┘
                │ service_role (claim/complete)       │ authenticated (모니터)
        ┌───────┴─────────┐  ┌──────────────────┐    │
        │ 프린트 스테이션 A │  │ 프린트 스테이션 B │    │
        │ print_agent.py   │  │ print_agent.py   │   [인쇄 관리자 페이지]
        │ +Chrome+Sumatra  │  │ +Chrome+Sumatra  │   (큐 모니터·재인쇄)
        │ → 프린터          │  │ → 프린터          │
        └──────────────────┘  └──────────────────┘
```

### 흐름
1. 학생: 스탠바이미에서 설문 → 결과 화면 도달
2. 결과 페이지: `enqueue_print` RPC 호출 → **출력번호(ticket)** 반환 → 화면에 "#137 프린트대에서 받아가세요"
3. 스테이션 A/B: `claim_next_job`으로 **빈 쪽이 작업 원자적 선점** → result 렌더 → PDF → 프린터 출력 → `complete_job`
4. 종이 나옴 (상단에 출력번호+프로필 → 학생/스태프가 식별)

---

## 3. Supabase 설계

### 3-1. 테이블

```sql
-- 큐레이션(핀/제외): 시험별 1행
create table curation (
  exam       text primary key,              -- '공무원' | '경찰' | '소방'
  data       jsonb not null default '{}',   -- 기존 admin_curation_v2 전체 구조
  updated_at timestamptz not null default now()
);

-- 인쇄 작업 큐
create table print_jobs (
  id         bigint generated always as identity primary key,
  exam       text not null,
  answers    jsonb not null,                -- passNoteAnswers (8슬롯/경찰 answersByQ)
  cur        jsonb,                         -- (선택) 인쇄 시점 큐레이션 스냅샷
  ticket     int,                           -- 출력번호(표시용)
  status     text not null default 'pending', -- pending|printing|done|error
  station    text,
  error      text,
  created_at timestamptz not null default now(),
  claimed_at timestamptz,
  printed_at timestamptz
);
create index print_jobs_status_idx on print_jobs (status, created_at);

-- 스테이션 상태(heartbeat)
create table stations (
  id        text primary key,               -- 'A' | 'B'
  last_seen timestamptz,
  busy_job  bigint
);

-- 출력번호 시퀀스
create sequence print_ticket_seq;
```

### 3-2. RPC (함수)

```sql
-- 결과 페이지(anon)가 인쇄 등록 → 출력번호 반환. 테이블 직접 접근 없이 이걸로만.
create or replace function enqueue_print(p_exam text, p_answers jsonb, p_cur jsonb)
returns int
language plpgsql security definer
as $$
declare t int;
begin
  t := nextval('print_ticket_seq');
  insert into print_jobs(exam, answers, cur, ticket) values (p_exam, p_answers, p_cur, t);
  return t;
end; $$;

-- 스테이션이 다음 작업을 원자적으로 선점 (2대가 같은 걸 못 가져감)
create or replace function claim_next_job(p_station text)
returns print_jobs
language plpgsql
as $$
declare job print_jobs;
begin
  -- 멈춘 작업 재큐: printing 60초 초과 → pending 복귀
  update print_jobs set status='pending', station=null, claimed_at=null
   where status='printing' and claimed_at < now() - interval '60 seconds';

  update print_jobs
     set status='printing', station=p_station, claimed_at=now()
   where id = (
     select id from print_jobs
      where status='pending'
      order by created_at
      for update skip locked          -- ← 핵심: 무충돌 작업큐
      limit 1
   )
  returning * into job;

  -- heartbeat 갱신
  insert into stations(id,last_seen,busy_job) values (p_station, now(), job.id)
    on conflict (id) do update set last_seen=now(), busy_job=excluded.busy_job;

  return job;   -- NULL이면 대기 작업 없음
end; $$;

create or replace function complete_job(p_id bigint)
returns void language sql as $$
  update print_jobs set status='done', printed_at=now() where id=p_id;
$$;

create or replace function fail_job(p_id bigint, p_err text)
returns void language sql as $$
  update print_jobs set status='error', error=p_err where id=p_id;
$$;

-- 관리자 재인쇄: error/done → pending 복귀
create or replace function requeue_job(p_id bigint)
returns void language sql as $$
  update print_jobs set status='pending', station=null, claimed_at=null, error=null where id=p_id;
$$;
```

### 3-3. RLS (보안 정책)

```sql
alter table curation   enable row level security;
alter table print_jobs enable row level security;
alter table stations   enable row level security;

-- 큐레이션: 공개 읽기 / 로그인(관리자)만 쓰기
create policy curation_read  on curation for select to anon, authenticated using (true);
create policy curation_write on curation for all    to authenticated using (true) with check (true);

-- 인쇄 작업: 직접 테이블 접근은 '로그인(관리자 모니터)' 읽기만.
--   - anon 등록은 enqueue_print(security definer)로만
--   - 스테이션 claim/complete는 service_role 키(RLS 우회)로
create policy jobs_admin_read on print_jobs for select to authenticated using (true);
create policy stations_read   on stations  for select to authenticated using (true);

-- anon이 호출 가능한 함수만 grant
grant execute on function enqueue_print(text,jsonb,jsonb) to anon, authenticated;
grant execute on function requeue_job(bigint) to authenticated;
-- claim/complete/fail 은 service_role(스테이션)만 — anon/authenticated grant 안 함
```

> 키 사용 정리
> - **anon key** (공개 가능): 결과/설문 페이지, curation 읽기, enqueue_print
> - **authenticated** (관리자 로그인): 큐레이션 쓰기, 인쇄 관리자 모니터/재인쇄
> - **service_role key** (절대 비공개, 스테이션 PC에만): claim/complete/fail

### 3-4. Auth
- Supabase Auth → 관리자 이메일/비번 1개 생성 (Dashboard에서 수동 추가).
- 관리자 페이지·인쇄 관리자 페이지에서 로그인 후 토큰으로 쓰기/모니터.

---

## 4. 프런트엔드 변경

### 4-0. 공통 — Supabase 클라이언트
`live/supa.js` 신규:
```html
<!-- 각 페이지 head -->
<script src="https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2"></script>
<script src="supa.js"></script>
```
```js
// supa.js
const SUPABASE_URL = 'https://xxxx.supabase.co';
const SUPABASE_ANON = 'eyJ...';   // anon key (공개 가능)
window.supa = supabase.createClient(SUPABASE_URL, SUPABASE_ANON);
window.CURRENT_EXAM = '공무원';   // 페이지/소스별로 설정
```

### 4-1. `curation_sync.js` — localStorage → Supabase
- `getStore()`를 **Supabase `curation` 테이블 읽기**로 교체 (메모리 캐시 + 최초 await).
- 실시간 반영: `supa.channel('curation').on('postgres_changes', {table:'curation'}, reload)` → 관리자가 핀 바꾸면 결과 페이지 자동 갱신.
- `applyPool / pinnedRandom / displayCount` 로직은 **그대로 유지** (입력만 Supabase 데이터로).
- 인쇄모드(`__PRINT_CUR__`) 주입 경로도 유지 (스테이션이 스냅샷 주입).

### 4-2. 관리자 페이지 `no01_폴백_매칭수.html`
- **로그인 게이트**: Supabase Auth 로그인 안 하면 편집 잠금(읽기만).
- `setStore()` → 로컬 대신 **Supabase `curation` upsert** (디바운스 0.5s).
- 미리보기/매트릭스/핀-랜덤/노출개수 UI는 그대로.

### 4-3. 결과 페이지 `result.html` / `police_result.html`
- 결과 도달 시(=loadAndRender 성공 후) **자동 인쇄 등록**:
```js
let _enqueued = sessionStorage.getItem('printTicket');
async function autoEnqueue(){
  if (_enqueued) return showTicket(_enqueued);             // 새로고침 중복 방지
  const { data, error } = await supa.rpc('enqueue_print', {
    p_exam: CURRENT_EXAM, p_answers: A, p_cur: null
  });
  if (!error) { sessionStorage.setItem('printTicket', data); showTicket(data); }
}
```
- 화면에 **"출력번호 #NNN — 프린트대에서 받아가세요"** 표시 + **"다시 인쇄"** 버튼(`enqueue` 재호출, ticket 갱신).
- 기존 로컬 `/print` POST(`sendPrint`)는 **로컬 테스트 전용**으로만 남김(클라우드에선 미사용).

### 4-4. JSON 슬림화 (선택, 권장)
- 빌드 스크립트로 결과에 쓰는 필드만 남긴 `*_min.json` 생성:
  - 필요한 필드: `id, title, tags, passage, quotes, year, job` (+ no.08용 `content`는 문장추출 때문에 큼 → **첫 달 문장만 미리 추출해 별도 필드**로 줄이는 것 권장)
- 목표: 공무원 5MB→~1.5MB, 경찰 10MB→~3MB. Netlify CDN 캐시로 2회차부터 즉시.

---

## 5. 프린트 스테이션 에이전트 `print_agent.py`

기존 `print_server.py`의 렌더/인쇄 로직 재활용. 차이: **HTTP 서버가 아니라 큐 폴링 클라이언트**.

```python
# 의사코드
import os, time, requests   # 또는 supabase-py
SUPABASE_URL = os.environ['SUPABASE_URL']
SERVICE_KEY  = os.environ['SUPABASE_SERVICE_KEY']   # 비공개! 이 PC에만
STATION_ID   = os.environ.get('STATION_ID', 'A')
SITE         = os.environ['SITE_URL']               # https://xxx.netlify.app

def rpc(fn, args):  # service_role로 RPC 호출
    return requests.post(f"{SUPABASE_URL}/rest/v1/rpc/{fn}",
        headers={'apikey':SERVICE_KEY,'Authorization':f'Bearer {SERVICE_KEY}',
                 'Content-Type':'application/json'}, json=args).json()

while True:
    job = rpc('claim_next_job', {'p_station': STATION_ID})
    if not job:
        time.sleep(1.5); continue
    try:
        # 1) 답변·큐레이션 스냅샷을 로컬 _printdata.json에 기록
        # 2) 헤드리스 크롬으로 SITE/live/result.html?print=1 렌더 → PDF
        #    (답변 주입: ?print=1 + _printdata.json, 기존 print_server 방식 그대로)
        # 3) SumatraPDF -print-to-default -silent → 프린터
        # 4) 완료
        render_and_print(job)                    # print_server.py render_pdf/print_pdf 재활용
        rpc('complete_job', {'p_id': job['id']})
    except Exception as e:
        rpc('fail_job', {'p_id': job['id'], 'p_err': str(e)})
```

> 주의
> - `?print=1` 결과 렌더 시 답변(`job.answers`)과 큐레이션 스냅샷 주입 방식은 기존 `result.html`의 인쇄모드(`_printdata.json` fetch) 재사용.
> - 출력물 상단에 **출력번호(ticket) + 프로필** 인쇄 (식별용) → result 인쇄모드에 ticket 표시 추가.
> - 스테이션 2대는 같은 코드, `STATION_ID`만 A/B로 다르게.
> - Chrome·SumatraPDF·기본 프린터 세팅은 각 스테이션 PC에 준비(기존과 동일).

---

## 6. 인쇄 관리자 페이지 (신규) `live/print_admin.html`

행사 중 인쇄 현황을 한눈에 보고 통제.

기능:
- **로그인** (Supabase Auth)
- **실시간 큐 현황**: pending / printing / done / error 카운트 (realtime 구독)
- **작업 목록**(최근 100): 출력번호·시험·상태·스테이션·시각·오류
- **재인쇄** 버튼: error/done → `requeue_job` 호출
- **스테이션 상태**: `stations.last_seen` 30초 이내면 🟢 온라인, 아니면 🔴 (다운 감지)
- (선택) 분당 처리량 그래프, 대기열 길이 경고(>10건이면 빨강)

구현 메모:
```js
// 실시간 구독
supa.channel('jobs')
  .on('postgres_changes', {event:'*', schema:'public', table:'print_jobs'}, refresh)
  .subscribe();
// 카운트
const { data } = await supa.from('print_jobs').select('status'); // 집계
// 재인쇄
await supa.rpc('requeue_job', { p_id });
```

---

## 7. Netlify 배포

1. GitHub 레포 연결 → 자동 배포 (push 시 갱신).
2. `netlify.toml`:
```toml
[build]
  publish = "."          # 정적 그대로 (또는 live/ 정리 후 publish="live")
[[headers]]
  for = "/*.json"
  [headers.values]
    Cache-Control = "public, max-age=86400"   # 후기 JSON 캐시
```
3. 공개 URL 확보 → 스탠바이미 브라우저 즐겨찾기/키오스크 모드로 설문 시작 페이지 고정.
4. 후기 JSON·이미지 경로 점검(상대경로). `police_result.html`은 `../police_reviews_data.json` → 배포 구조에 맞게 조정 필요(루트 publish면 OK).

> 참고: Netlify 무료 100GB/월. 500명×슬림JSON(~3MB)=~1.5GB → 여유.

---

## 8. 행사 운영 체크리스트

**사전**
- [ ] Supabase 프로젝트 생성 → 스키마 SQL 실행 → 관리자 계정 생성
- [ ] anon/service_role 키 확보 (service_role은 스테이션에만)
- [ ] Netlify 배포 + 공개 URL 확인 (실기기=스탠바이미 webOS 브라우저에서 설문~결과 테스트)
- [ ] 후기 JSON 슬림화 적용
- [ ] 큐레이션(핀) 사전 작업 완료 (관리자 페이지)
- [ ] 스테이션 2대: Python+Chrome+SumatraPDF 설치, 기본 프린터 지정, `print_agent.py` 환경변수 세팅, 시범 인쇄 OK
- [ ] 인쇄 관리자 페이지에서 스테이션 A/B 🟢 확인

**당일**
- [ ] 행사장 인터넷 안정성 확인(가능하면 유선)
- [ ] 스탠바이미 키오스크 모드(설문 URL 고정, 뒤로가기 잠금)
- [ ] 프린터 용지·토너 충분 (500장+)
- [ ] 인쇄 관리자 페이지 상시 모니터 (대기열·실패·스테이션 다운)

**문제 대응**
- 스테이션 1대 다운 → 나머지 1대가 큐 계속 소진(자동). 관리자 페이지에서 🔴 확인.
- 출력 누락 → 관리자 페이지에서 해당 ticket **재인쇄**.
- 인터넷 끊김 → 복구 시 큐 자동 재개(작업 보존). 단 신규 설문 등록은 끊긴 동안 불가.

---

## 9. 작업 순서 (사무실 TODO)

1. **Supabase**: 프로젝트 생성 → §3 SQL 실행 → 관리자 계정 → 키 확보
2. **supa.js** 추가 + 각 페이지 로드
3. **curation_sync.js**: Supabase 읽기/실시간으로 교체 (로직 유지)
4. **관리자 페이지**: 로그인 게이트 + setStore→Supabase upsert
5. **결과 페이지**: 자동 enqueue + 출력번호 표시 + 다시인쇄 버튼
6. **print_agent.py**: print_server.py 기반 큐 폴링 버전 작성 + 2대 세팅
7. **print_admin.html**: 큐 모니터/재인쇄/스테이션 상태
8. **JSON 슬림화** 스크립트
9. **Netlify 배포** + netlify.toml + 실기기 테스트
10. 리허설(소수 인원으로 설문→인쇄 풀 사이클)

---

## 10. 리스크 & 대비

| 리스크 | 영향 | 대비 |
|--------|------|------|
| 행사장 인터넷 불안정 | 설문·인쇄 전체 영향 | 유선/전용망, 모바일 라우터 백업 |
| 스탠바이미 webOS 브라우저 호환 | 레이아웃/JS 깨짐 | **반드시 실기기 사전 테스트** (폰트·CSS·fetch) |
| service_role 키 노출 | 보안 사고 | 스테이션 PC 로컬에만, 절대 프런트/깃에 두지 않음 |
| JSON 과대(첫 로딩 느림) | 태블릿 체감 저하 | 슬림화 + CDN 캐시 |
| 프린터 병목 | 대기 발생 | 2대 + 레이저(고속), 관리자 대기열 모니터 |
| 중복/누락 출력 | 종이 낭비/누락 | SKIP LOCKED 큐 + 세션 dedup + 재인쇄 기능 |

---

## 부록 A. 현재 코드 재활용 매핑

| 신규 | 재활용 출처 |
|------|------------|
| `print_agent.py` 렌더/인쇄 | `print_server.py` 의 `render_pdf()`, `print_pdf()`, `find_chrome()`, `find_sumatra()` |
| 결과 인쇄모드 | `result.html` 의 `?print=1` + `_printdata.json` 주입 로직 |
| 큐레이션 적용 | `curation_sync.js` 의 `applyPool/pinnedRandom/displayCount` (입력만 교체) |
| 관리자 큐레이션 UI | `no01_폴백_매칭수.html` (저장소만 Supabase로) |

## 부록 B. 로컬 vs 클라우드

| 기능 | 로컬(현재) | 클라우드(이 설계) |
|------|-----------|------------------|
| 정적 서빙 | `python http.server` / `print_server.py` | Netlify |
| 큐레이션 저장 | localStorage(브라우저 한정) | Supabase(공유) |
| 인쇄 | 그 PC 프린터(`print_server`) | 스테이션 2대 + 클라우드 큐 |
| 인증 | 없음 | Supabase Auth |

> 로컬 `print_server.py`는 개발/테스트용으로 계속 유지. 클라우드는 위 구조 별도.
