# 경찰 후기 매칭 — 인수인계 / 할 일

> 작성: 2026-06-27 · 경찰 결과 리포트(설문→맞춤 합격수기) 작업
> **현황 한 줄**: 경찰 결과페이지·큐레이션·2면 다 만들어 **로컬에서 동작**. 라이브(Vercel/Supabase)엔 **아직 안 올림**. 남은 건 ① 검수 마무리 ② 강사 확정 ③ 배포.

---

## ⚠️ 0. 지금 어디에 있나
| 위치 | 경찰 작업 반영? |
|---|---|
| 내 PC 로컬 서버(8181) | ✅ 다 보임 |
| 라이브 Vercel(surveyandresult.vercel.app) | ❌ 옛버전 (git push 안 함) |
| Supabase 큐레이션 | ❌ 비어있음 |

→ **마음껏 만져도 라이브 영향 0.** 올리려면 §4 배포 단계 따로 해야 함.

서버 켜는 법(꺼져 있으면): 폴더에서 `python -m http.server 8181`
- 결과페이지: http://localhost:8181/live/police_result.html
- 검수페이지: http://localhost:8181/live/police_curate_confirm.html
- 관리자: http://localhost:8181/live/police_admin.html

---

## ✅ 1. 남은 할 일 (집에서 — 우선순위 순)

### [1] 후기 검수 마무리 (사람)
- `police_curate_confirm.html` 열기 → 상단 영역 드롭다운에 5개 있음.
- **no.01·06은 이미 검수 끝남(122핀).** **no.07(그 고민)·no.08(첫 한 달)·no.09(포기 직전) 3개만 마저 검수.**
- 방법: 조합별 카드 보고 **이상한 것만 클릭해 빨강(제외)**, `Enter`로 다음. (기본 전체 초록=확정)
- ⚠️ **no.07 검수 땐 검정 본문 말고 "파란 인용 글" 보고 판단** (그게 실제 노출되는 답).
- 다 끝나면 → **`📤 백업 내보내기`** → `경찰_curation.json` 새로 저장(다운로드 폴더).

### [2] 강사 검수목록 확정 (사람 — 해커스 강사만 남기기)
- `경찰_강사교재_검수목록.md` 열기.
- `❓` 표시된 강사가 **해커스면 `✅`, 타사면 `❌`** 로 바꾸기. (특히 경찰학 김재규·김민철·이상훈, 면접 문영미·최준 등 — 해커스 강사인지 확인 필요)
- ⛔ **이미 ❌ 처리됨(타사): 박철한·황남기(헌법), 천재근(면접) — 절대 ✅ 금지.**
- ✅ 확정된 것(해커스): 형사법 김대환·경찰학 조현·헌법 신동욱·범죄학 노신·영어 비비안.
- 마크 끝나면 → 터미널에서 `python build_police_stats.py` 재실행 → 결과페이지 2면 강의카드에 반영됨.

### [3] 결과페이지 눈으로 점검
- `police_result.html` **새로고침 여러 번** 하며:
  - 후기가 매번 바뀌나? (랜덤 적용됨)
  - 한 장 안에서 같은 후기 중복 안 나나?
  - 빈칸/깨진 곳 없나? (전형=경찰간부, 성별=여성도 확인)
- 설문 화면(`police_survey.html`)부터 한 바퀴 돌려서 자연스러운지.

### [4] 배포 (라이브 올리기) — §4 참조

### [5] (보류) 상황 태그 보강 — 나중에
- 데이터에 "전업/직장/재학" 등 상황 태그가 거의 없어, Haiku로 후기 본문 읽어 상황 분류하는 작업(약 $0.5). **지금은 안 함.** 자세한 건 메모리/`경찰_결과매칭_점검.md`.

---

## 2. 완성된 것 (파일별)

### 결과페이지
- **`live/police_result.html`** — 경찰 맞춤 리포트(1면+2면). 핵심 로직:
  - 1면: 전형×성별 매칭 + **걱정·방식 가중**(같은 전형이어도 고민 다르면 다른 후기).
  - **전 영역 풀에서 랜덤** + 영역 간 중복제거(500명이 다 다른 리포트).
  - 2면: 합격자 전략(no.12) + **합격자가 들은 강의=해커스 강사만, 숫자 없음**(no.14).
  - 빈칸 폴백(경찰간부·여성), 타이틀 경찰화 완료.
  - **인쇄 연동 완료**: 출력 버튼 → `enqueuePrint('경찰', A, null)`(Supabase 큐) + 출력번호 표시(없으면 로컬 /print 폴백, 키 `admin_curation_v2_경찰`). 인쇄모드(`?print=1`)에 **출력번호 배지(`#print-ticket`)** + cur 없을 때 `Curation.loadRemote()` 폴백. → 서버/배포 터미널 인쇄 큐와 연결됨(print_agent EXAM_PAGE에 경찰 매핑 있음).

### 큐레이션(검수)
- **`live/police_admin.html`** — 매트릭스형 관리자. 저장키 `admin_curation_v2_경찰`.
- **`live/police_curate_confirm.html`** — 카드형 검수(주력). export `경찰_curation.json`.
- **`auto_curate_police.py`** → `police_auto_curation.json` — 자동 핀 후보.
- **`build_police_candidates.py`** → `live/police_curate_candidates.json` — 검수 후보(no.01·06·07·08·09).

### 2면 데이터
- **`build_police_stats.py`** → `live/police_stats.json` — 과목별 해커스 강사 + 전략 문장.
  - 검수목록 MD의 ✅/❌를 읽어 반영. 강사명 OCR 별칭 병합(갓대환→김대환 등).
- **`경찰_강사교재_검수목록.md`** — 강사 검수 surface.

### 데이터 원본
- `police_reviews_data.json` (루트, 1,337건) / `police_reviews.db` (전체 본문 content 보유).

---

## 3. 실행 명령 (터미널, 폴더 안에서)
```
python build_police_candidates.py   # 검수 후보 재생성(매칭 로직 바꿨을 때)
python build_police_stats.py        # 2면 강사/전략 재집계(검수목록 고친 뒤)
python auto_curate_police.py        # 자동핀 재생성(필요 시)
```

---

## 4. 배포 방법 (라이브 올리기 — 두 단계 별개)

### A. 코드 → Vercel
- `git status`로 확인 → **경찰 파일만** 골라서 add (⚠️ `git add -A` 금지: 남의 미커밋 섞임).
  ```
  git add live/police_result.html live/police_admin.html live/police_curate_confirm.html \
          live/police_survey.html live/police_stats.json live/police_curate_candidates.json \
          auto_curate_police.py build_police_candidates.py build_police_stats.py \
          police_auto_curation.json 경찰_강사교재_검수목록.md
  git commit -m "경찰 결과페이지 매칭·2면·검수 완성"
  git push        # → Vercel 자동 배포
  ```
- push 전 `git pull` 먼저(다른 터미널 작업 있음).

### B. 큐레이션 → Supabase
- **서버/배포 터미널 영역.** `경찰_curation.json` 준비되면 그쪽에 넘기거나,
- `police_admin.html`에서 ☁ 서버저장 버튼(있으면) 눌러 Supabase `exam='경찰'` 행에 올림.

---

## 5. 핵심 주의사항
- **저장키 분리**: 경찰=`admin_curation_v2_경찰`, 공무원=`admin_curation_v2`. 안 섞임.
- **강사**: 언급수 1위 ≠ 해커스. 헌법 박철한(최다)은 **타사**. 반드시 해커스만.
- **숫자 노출 금지**: 결과에 N%·N명 표시 X(이미 2면 % 막대 제거함). 순위/정성표현만.
- **공무원 파일 원본 수정 금지**: result.html, no01_폴백_매칭수.html, auto_curate.py 등은 복제만.
- 다른 터미널과 조율: `터미널간공유문서.md`.

---

## 6. 참고 문서
- `경찰_결과매칭_점검.md` — 매칭 영역별 점검 + 수정 내역
- `경찰_2면_기획.md` — 2면 구성 기획
- `인수인계_경찰작업.md` — 최초 인수인계(설문 슬롯/태그맵 등 상세)
- `터미널간공유문서.md` — 터미널 간 작업 로그
