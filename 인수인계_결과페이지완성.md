# 인수인계 — 결과 페이지 완성 (공무원, 모든 질문 대응)

> 받는 사람: **"결과페이지 완성" 터미널**
> 보내는 사람: "서버/배포(Supabase·Vercel)" 터미널
> 작성일: 2026-06-26 · 기준 커밋: `242366a`

---

## 0. 당신의 미션 (한 줄)
**`live/result.html`(공무원)을 "모든 설문 답변 조합에서 빈칸·더미 없이 완성"** 시킨다.
지금 동적 매칭은 대부분 되지만 **no.12/13/14가 정적 더미**이고, 일부 조합에서 빈 영역/약한 매칭이 날 수 있다. 그걸 **전부 메우는** 작업.

---

## 1. ⛔ 경계선 (두 터미널 충돌 방지 — 꼭 지킬 것)

| 구분 | 파일/영역 | 누가 |
|---|---|---|
| ✅ **당신이 작업** | `live/result.html`, 결과 렌더 로직, no.12/13/14 동적화, 신규 집계 스크립트(예: `build_*_stats.py`), 타이틀/카피 | 결과페이지 터미널 |
| ⛔ **건드리지 말 것** | `curation_sync.js`(서버터미널이 **Supabase로 이전 중**), `vercel.json`/`.vercelignore`, `supa.js`, `supabase/*.sql`, `print_agent.py`, 관리자 페이지의 **로그인/저장소** 부분 | 서버 터미널 |
| 🤝 **건드리려면 먼저 알릴 것** | `no01_폴백_매칭수.html`(관리자 페이지)에 **새 영역(no.12~14 등) 추가**, `curation_sync.js`의 `AREAS_DEFAULT`에 영역 추가 | 둘 다 관여 → 조율 |

### 충돌 규칙
1. **Git push는 서버 터미널이 담당.** 당신은 commit까지 하고 "push해줘" 라고 알리거나, push 전 반드시 `git pull --rebase`.
2. **dev 서버(8181)는 한 곳만** 실행. 이미 떠 있으면 그 브라우저로 접속만. (현재 서버 터미널이 `python -m http.server 8181` 실행 중)
3. **`curation_sync.js`는 절대 수정 금지.** 그 API(`Curation.applyPool / pinnedRandom / displayCount / comboFromAnswers / onChange`)는 **그대로 호출만** 한다. 저장소(localStorage→Supabase)는 서버터미널이 바꾼다 — 당신 코드는 영향 없음(API 동일).
4. **큐레이션 데이터(핀)는 지금 브라우저 localStorage**에 있다. 결과페이지 테스트는 같은 출처(localhost:8181)에서 해야 핀이 보인다.

---

## 2. 먼저 읽을 문서 (이미 있음, 중복 작성 X)
- **`결과페이지_항목정리.md`** ← **가장 중요.** no.01~15 전 항목의 매칭키·로직·동적/정적·개선안 총정리. 이걸 스펙으로 삼아라.
- `결과페이지_마스터.md` — 초기 설계/카피 룰(숫자 노출 금지 등 하드룰 포함).
- `클라우드_인쇄시스템_설계.md` — 전체 배포 그림(당신 작업과 직접 관련 적음, 맥락용).

> **하드룰(마스터 문서)**: ❌우리가 집계한 숫자(N명·N%) 노출 금지 / ⭕후기 원문 숫자는 OK / ⭕"다수의·여러" 같은 정성 표현. → no.12/14의 더미 `%`가 이 룰 위반이라 **동적화 또는 정성표현 전환**이 필요.

---

## 3. 현재 상태 (코드 기준, `result.html`)

| 항목 | 위치 | 매칭키 | 상태 |
|---|---|---|---|
| no.01 대표후기 | S1좌 | Q1×Q3×Q4 (7폴백) | 🟢 동적 + 큐레이션(pinnedRandom) |
| no.02 히어로쿼트 | S1좌 | Q5×Q7×Q8 (7폴백) | 🟢 동적 |
| no.06 같은출발점 | S1우 | Q1×Q4 | 🟢 동적 + 큐레이션(displayCount/pinnedRandom) |
| no.07 다잡은한마디 | S1우 | Q8 | 🟢 동적 (Q8 미선택 시 빈 표시) |
| no.08 첫한달풍경 | S1우 | Q1×Q3 | 🟢 동적 (content 문장 추출) |
| no.09 포기직전 | S1우 | Q1+슬럼프KW | 🟢 동적 |
| no.10 꾸준함한줄 | S1우 | Q1~Q8(≥5) | 🟢 동적 |
| no.11 유사후기 3카드 | S2상 | Q1~Q8 전체 | 🟢 동적 |
| **no.12 후회** | S2하 | Q3 (행정직/_default만) | 🔴 **정적 더미** |
| **no.13 첫 달** | S2하 | Q1×Q3 (_default만) | 🔴 **정적 더미** |
| **no.14 어려운과목** | S2하 | Q3 (행정/세무/교육/_default) | 🔴 **정적 더미(%)** |
| no.15 CTA쿼트 | S2푸터 | Q1~Q8(≥5) | 🟢 동적 |

- 큐레이션 연동: `render()` 안에서 `const C = (area)=>Curation.applyPool(area,combo,ps)` 로 후보풀을 감싸고, no.01/no.06은 `pinnedRandom`+`displayCount` 사용. **관리자 핀이 자동 반영됨.**
- 정적 더미: `REGRET_BY_Q3`(757행대), `FIRSTMONTH_BY_Q1Q3`(767), `HARD_SUBJECT_BY_Q3`(775).
- 인쇄모드(`?print=1`)·A4 2-up 레이아웃 존재. **레이아웃은 A4 고정** — 내용 길어도 안 넘치게 `line-clamp` 유지.

---

## 4. 해야 할 일 (우선순위)

### 🥇 1순위 — no.12/13/14 정적 → 동적/정성화
지금 직렬 8개 중 대부분이 `_default` 더미. **모든 Q3(직렬)에서 의미 있는 출력**이 나오게.
- **no.12 후회**: 후기 `content`에서 후회/아쉬움 문장 집계(직렬별) 또는 **큐레이션으로 직렬별 핀** 받기. 더미 `%` 제거 → "다수가 언급" 같은 정성표현.
- **no.14 어려운 과목**: 후기 본문/태그에서 과목 난이도 언급 집계(직렬별). 막대 `%`가 더미면 **실제 집계 전엔 수치 숨김**.
- **no.13 첫 달**: no.08(첫 한 달 감성)과 중복 → **체크리스트형으로 차별화**하거나 통합 검토.
> 집계가 무겁다면, **관리자 큐레이션(핀)으로 직렬별 항목을 사람이 골라 넣는 방식**도 가능(=서버터미널이 영역 추가하면 됨, §1 조율).

### 🥈 2순위 — 모든 조합에서 "빈칸/약한 매칭" 제거
- Q8 미선택 시 no.07이 비는 등 **빈 상태 처리**가 자연스러운지 점검.
- 폴백 마지막 단계에서도 1건은 나오는지(특히 희소 직렬: 간호보건/사회복지).
- **모든 프리셋 + 직렬 8 + Q8 6종**을 돌려 빈칸/어색 매칭이 없는지 전수 확인(§6 체크리스트).

### 🥉 3순위 — 타이틀/카피 다듬기 (항목정리 §6A)
- `수험 정체` → `나의 수험 프로필`
- 히어로쿼트 위 라벨 `이 길을 먼저 걸은 사람의 한마디`
- no.13 `합격생의 첫 달 체크리스트`

### ⭐ 선택 — 신규 항목 (항목정리 §6C·D)
- **교재·강사 TOP**: `reviews.db`의 `textbooks`/`instructors` 테이블이 **미사용** 상태로 존재 → 실용 정보 신설 가능.
- 합격 직후 한마디 / 하루 루틴 한 줄 / 같은 직렬 합격 연도 분포 등.

---

## 5. 알아야 할 구조

### 5-1. 설문 8문항 + 전체 선택지 (tagId ⇒ 라벨)
- **Q1 상황**: 전업수험생⇒전업 수험생 · 직장병행⇒직장 다니면서 · 재학수험생⇒학교 다니면서 · 육아병행⇒육아하면서 · 퇴직수험생⇒은퇴·퇴직 후 · 아르바이트병행⇒알바하면서
- **Q2 시험**: 국가직9급 · 지방직9급 · 서울시9급
- **Q3 직렬**: 행정직 · 세무직 · 공안직 · 교육직 · 기술직 · 간호보건직 · 사회복지직 · 기타직렬
- **Q4 베이스**: 노베이스 · 수능베이스 · 전공자 · 토익베이스
- **Q5 기간**: 1년이내 · 1년이상
- **Q6 시간**: 하루10시간미만(5미만/5~8) · 하루10시간이상(8~10/10이상) ※라벨 4개·tagId 2개
- **Q7 방식(복수)**: 인강병행 · 기출반복 · 회독반복 · 독학위주 · 노량진현강 · 스터디병행
- **Q8 고민**: 멘탈관리 · 슬럼프극복 · 영어약자 · 효율중심 · 이해중심학습 · 커리큘럼중심
- 답변은 `passNoteAnswers`(8슬롯, Q7만 배열). 후기 매칭은 `TAG_EXPANSION`(result.html 상단)으로 tagId→실제 태그 확장 후 비교.

### 5-2. 큐레이션 API (curation_sync.js — 호출만, 수정 X)
- `Curation.comboFromAnswers(A)` → `{Q1..Q8}`
- `Curation.applyPool(area, combo, pool)` → 핀 있으면 핀된 것만/없으면 제외만 반영한 풀
- `Curation.pinnedRandom(area, combo, pool, n)` → 핀된 것 중 랜덤 n개(없으면 null=자동로직)
- `Curation.displayCount(area)` → 영역 노출 개수(관리자 설정)
- `Curation.onChange(cb)` → 관리자가 핀 바꾸면 재렌더
- 영역코드: `no.01,no.02,no.06~no.10` (현재 AREAS_DEFAULT). **no.12~14를 큐레이션 대상으로 하려면 AREAS_DEFAULT 추가 필요 → 서버터미널과 조율(§1).**

### 5-3. 데이터
- `live/reviews_data.json` (공무원 **2,375건**, 태깅 완료). 필드: `id,title,tags,passage,quotes,year,job,content,inst,book` 등.
- `reviews.db` (sqlite): `passnotes / tags / passnote_tags / instructors / textbooks`. **instructors·textbooks는 결과페이지 미사용** → 신규 항목용 자산.
- 집계 스크립트 만들면 `reviews.db`에서 뽑아 `*_min.json` 류로 내보내는 방식 권장(서버터미널의 JSON 슬림화와 파일명 안 겹치게 조율).

---

## 6. 완료 검증 체크리스트 (이게 "완성" 기준)
- [ ] **프리셋 5종** 전부 결과가 자연스럽고 빈칸 없음
- [ ] **Q3 직렬 8종** 각각에서 no.12/13/14가 의미 있는 출력(더미 아님)
- [ ] **Q8 6종** 각각 no.07이 주제에 맞는 인용
- [ ] **희소 직렬(간호보건/사회복지)** + 노베이스 등 조합에서도 폴백으로 1건 이상
- [ ] Q8 미선택/Q7 미선택 등 **빈 입력**에서도 깨지지 않음
- [ ] **집계 숫자(%) 노출 금지** 룰 준수 (더미 % 제거 또는 정성표현)
- [ ] **A4 2-up 인쇄 레이아웃** 안 넘침(`?print=1`로 확인) · 화면 2장 정상
- [ ] 관리자에서 핀 변경 시 결과페이지 **자동 반영**되는지(onChange)

---

## 7. 시작 전 확인할 것 (서버 터미널에 물어볼 것)
1. **지금 push해도 되는지** / 내가 commit만 하고 너가 push할지 (절차 합의)
2. **curation_sync.js Supabase 이전이 끝났는지** — 이전 후엔 핀 데이터가 Supabase에 있으니, 내 로컬 테스트에서 핀이 안 보이면 그 때문일 수 있음
3. no.12~14를 **집계 동적화 vs 큐레이션 핀** 중 어느 방향으로 갈지(큐레이션이면 영역 추가를 서버터미널이 해줘야 함)
4. 신규 집계 스크립트/JSON 파일명이 서버터미널 슬림화와 안 겹치는지

---

## 8. 빠른 시작
```bash
git pull --rebase            # 최신부터
# 서버는 이미 8181에 떠 있음 → 브라우저로:
#   http://localhost:8181/live/index.html      (설문)
#   http://localhost:8181/live/result.html     (결과 — 여기가 작업 대상)
#   http://localhost:8181/live/no01_폴백_매칭수.html  (관리자 큐레이션)
# 결과만 빨리 보려면 result.html?print=1 로 인쇄 레이아웃도 확인
```
> 작업 끝나면: commit → (서버터미널에 알리고) push. `curation_sync.js`·배포파일은 절대 안 건드렸는지 `git diff --stat`로 확인.

---

## 9. 추가 지시 — 사용자 피드백 (2026-06-26)

### 9-1. 큐레이션 = 방법 A (칸마다 별도 풀) 확정
- **각 항목(칸)이 원하는 후기 "성격"이 다르다** → 항목마다 따로 핀(공유 X).
- 조합 기준은 **상황(Q1) × 직렬(Q3)** 중심 (예: `직장 × 일반행정`), 조합당 약 10개.
- `curation_sync`는 이미 (area, combo) 단위 핀이라 구조 변경 불필요. (서버터미널)

### 9-2. 항목별 후기 "성격" 가이드 (가장 중요 — 매칭/큐레이션 기준)
| 항목 | 보여줄 후기 성격 | 예시 톤 |
|---|---|---|
| no.01 대표후기(좌 히어로) | **감정/격려형** — "막막했지만 해냈다" | 동일시·위로 (현재 OK) |
| **no.06 같은 출발점** | **🔴 실전형으로 바꿔야 함** — "어떤 베이스에서 어떻게 공부했나" (노베라 이렇게 시작 / 수능베이스라 이건 수월·이렇게) | **학생이 실제 방법 도움받게** |
| no.11 유사후기 | 종합 유사 합격자 | 종합 |
| no.07/09/10/15 | 주제별 한 마디(인용) | 짧은 격려 |

### 9-3. no.06 구체 수정 (결과페이지 터미널 작업)
1. **박스 2개 → 1개**: `curation_sync`의 `DEFAULT_SHOW['no.06']=1`로 서버터미널이 이미 변경함. result.html의 `renderNo06`/레이아웃이 **카드 1개**로 자연스럽게 나오는지 확인(2칸 그리드면 1개일 때 정렬 점검).
2. **auto 선정 키워드를 "실전형"으로 retune**: 현재 `renderNo06`의 `KW_VIBE = [처음·막막·시작·다행·됐·버텼·꾸준·…]` = **감정형** → 학생에게 도움 안 됨.
   - 실전형 키워드로 교체/보강 권장: `공부법·루틴·회독·기출·단권화·계획·시간표·순서·전략·이렇게·방법·베이스·수월·노베이스라·강의·인강` 등.
   - 가능하면 passage에서 "방법/순서" 문장이 든 후기를 우선.
   - (단, 사용자가 no.06에 직접 핀하면 그 핀이 auto를 override하므로 핀된 조합은 자동으로 해결됨.)

> 나쁜 예(실제로 no.06에 떴던 것): "공부를 시작한 계기는 부모님의 권유였습니다. 처음엔 막막했지만 꾸준히…" → 이건 no.01(히어로)용. no.06엔 부적합.
