# 🏢 사무실 오늘 할 일 — 수동 인쇄 셋팅 (2026-06-27)

> 결정: **행사장은 "전부 수동" 운영.** (클라우드 자동분배 안 씀)
> 흐름: 태블릿 설문→결과→[출력] → Supabase `print_jobs` 적재 → **프린트대 PC에서 목록 보고 직접 골라 출력**
> 이 문서만 순서대로 따라 하면 됨. 위에서부터 차례로.

---

## 0. 최신 코드 받기
- [ ] 사무실 PC 레포 폴더에서 **`git pull`**
      → `print_station_manual.bat`, `print_agent.py`, `tools\SumatraPDF.exe` 등 전부 따라옴

## 1. 준비물 점검 (이 PC)
- [ ] **Chrome** 설치돼 있음 (렌더 엔진)
- [ ] **`tools\SumatraPDF.exe`** 있음 (git pull로 따라옴 — 확인만)
- [ ] **프린터 2대** 연결 + 둘 다 인쇄 테스트지 한 장씩 나옴
- [ ] 각 프린터 **정확한 이름** 확인 → 제어판 **「장치 및 프린터」** 에 뜨는 이름 그대로 메모
      (예: `Canon G3000 series`, `EPSON L3150 Series`)

## 2. service_role 키 확보
- [ ] Supabase 대시보드 → **Project Settings → API**
- [ ] **`service_role` (secret)** 항목 **Reveal → 복사**
      ⚠️ `anon` 아님! `service_role`(비공개)이어야 출력이 됨. 이 키는 이 PC에만.

## 3. station_env.bat 만들기 (키·프린터 입력)
- [ ] 레포 폴더의 **`station_env.bat.example`** 복사 → 이름을 **`station_env.bat`** 로 변경
- [ ] 메모장으로 열어 **3개만** 채우기:
  - `SUPABASE_SERVICE_KEY=` 뒤에 2번에서 복사한 **service_role 키**
  - `PRINTER_A=` 프린터1 정확한 이름
  - `PRINTER_B=` 프린터2 정확한 이름
- [ ] 저장
  > `SUPABASE_URL` 은 이미 채워져 있음. `STATION_ID`·포트는 bat가 자동 설정(손 안 댐).
  > ⚠️ `station_env.bat` 은 키가 들어있어 git에서 자동 제외됨 — 커밋·웹업로드 금지(자동 처리됨).

## 4. 실행 & 테스트 (한 바퀴)
- [ ] **`print_station_manual.bat` 더블클릭**
      → 검은 창 뜨고, 잠시 후 브라우저에 출력 목록(`http://127.0.0.1:8250/__pick`) 자동 오픈
- [ ] **빠른 테스트(태블릿 없이):** Supabase **SQL Editor** 에서 아래 1줄 RUN
      ```sql
      select enqueue_print('공무원', '[]'::jsonb, null, '테스트');
      ```
      → 목록에 새 줄이 뜸 → **[🖨 A로]** 클릭 → 프린터 A에서 종이 나오면 성공
- [ ] **[🖨 B로]** 도 한 번 눌러 프린터 B 정상 확인
- [ ] (선택) 실제 태블릿/PC에서 라이브 사이트 설문→결과→**[출력]** → 목록에 떠서 출력되는지 확인

## 5. 행사 당일 운영
- [ ] PC 켜고 **`print_station_manual.bat`** 실행(검은 창 + 목록 페이지 켜두기)
- [ ] 목록 상단 **「대기만」 체크** → 아직 안 뽑은 것만 보임(빠뜨림 방지)
- [ ] 설문 들어올 때마다 줄이 생김 → 그 사람 줄에서 **[🖨 A로]/[🖨 B로]** 중 **한가한 프린터** 클릭
- [ ] 프린터 용지·토너 넉넉히(500장+)

---

## ❗ 문제 대응
- **목록이 안 뜸/비어있음** → 검은 창에 에러 메시지 확인. 보통 `station_env.bat` 의 service_role 키 오타.
- **버튼 눌러도 출력 안 됨** → 프린터 이름이 「장치 및 프린터」와 정확히 같은지, 기본 프린터 켜져 있는지 확인.
- **'실패' 표시** → 해당 줄 다시 누르면 재시도(중복 출력은 자동 방지됨).
- **창 닫으면 멈춤** → 행사 동안 검은 창은 계속 켜둘 것.

## 🧩 안 해도 되는 것 (수동 운영이라 생략)
- `supabase/migration_realtime.sql` 실행 ❌ (자동 모니터용 — 수동엔 불필요)
- 자동 모드(`print_station.bat` / `print_station_2printers.bat`) ❌
  (나중에 자동 쓰고 싶으면 그때 전환. 코드는 그대로 있음)

## 📌 참고 값
- Supabase URL: `https://zxjhgcumweqaxcemoiga.supabase.co`
- 관리자 로그인: `minbum01@gmail.com`
- 수동 출력 목록 주소: `http://127.0.0.1:8250/__pick`
- 자세한 배경: `SUPABASE.md`, `클라우드_인쇄시스템_설계.md`
