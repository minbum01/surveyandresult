# 배포 가이드 — Vercel + Supabase

> 담당: 서버/배포 터미널 · 기준 설계: `클라우드_인쇄시스템_설계.md`
> 이미 만들어 둔 파일: `vercel.json`, `.vercelignore`, `supabase/schema.sql`, `live/supa.js`

---

## 단계 0. 지금 바로 가능 — 정적 사이트만 Vercel 배포 (Supabase 없이도 동작)
현재 사이트는 localStorage 큐레이션으로 그대로 돈다. URL부터 확보하고 Supabase는 다음 단계.

1. https://vercel.com → GitHub로 로그인 → **Add New → Project** → `minbum01/surveyandresult` 선택
2. 설정(거의 기본값):
   - Framework Preset: **Other** (빌드 없음 / 정적)
   - Build Command: **비움**, Output Directory: **비움**(루트)
   - `vercel.json`이 헤더·리다이렉트(`/` → `/live/home.html`) 자동 적용
3. **Deploy** → `https://<프로젝트>.vercel.app` 확보
4. 확인: `/live/home.html`, `/live/index.html`, `/live/result.html` 정상 + 후기 JSON 로드(네트워크 탭)
5. push할 때마다 자동 재배포됨

> `.vercelignore`로 .py/.db/원본JSON/tools 등은 안 올라감(용량↓). 페이지가 fetch하는 `live/*_reviews_data.json`·이미지·폰트는 유지됨.

---

## 단계 1. Supabase 프로젝트
1. https://supabase.com → New project (지역 가까운 곳, 예: Seoul/Tokyo)
2. **SQL Editor** → `supabase/schema.sql` 전체 붙여넣고 **RUN** (테이블·RPC·RLS 생성)
3. **Authentication → Users → Add user** → 관리자 이메일/비번 1개 생성
4. **Project Settings → API** 에서 3개 확보:
   | 키 | 용도 | 비밀? |
   |---|---|---|
   | Project URL | 전부 | 공개 OK |
   | **anon public** | 프런트(결과/설문/관리자 읽기·enqueue) | 공개 OK |
   | **service_role** | **스테이션 PC만** (claim/complete) | 🔴 절대 비공개·깃/프런트 금지 |
5. `live/supa.js` 상단 `SUPABASE_URL` / `SUPABASE_ANON` 채우기 (anon만!)

---

## 단계 2. 프런트 Supabase 연동 (⚠ result.html은 "결과페이지 터미널"과 조율)
> `curation_sync.js`는 이쪽 소유지만, `result.html`은 결과페이지 터미널이 작업 중 → **그쪽 후기 작업 끝나고 핀 백업 후** 진행.

순서:
1. **핀 백업**: 관리자 페이지에서 현재 localStorage 큐레이션을 **📥 핀 내보내기(JSON)** → Supabase `curation` 테이블 `data`에 주입(공무원 행).
2. **각 페이지 head에 추가** (supabase-js CDN + supa.js):
   ```html
   <script src="https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2"></script>
   <script src="supa.js"></script>
   ```
   (대상: home·index·result·police_result·관리자 페이지)
3. **`curation_sync.js`**: `getStore()`를 Supabase `curation` 읽기로 교체(메모리 캐시 + realtime 구독). `applyPool/pinnedRandom/displayCount`는 그대로. → 설계 §4-1
4. **관리자 페이지**: 로그인 게이트 + `setStore()`→Supabase upsert(디바운스). → §4-2
5. **결과 페이지**: 결과 도달 시 `enqueue_print` 자동 호출 + 출력번호 표시 + "다시 인쇄". → §4-3
6. **JSON 슬림화**(권장): result용 필드만 남긴 `*_min.json`. → §4-4

---

## 단계 3. 프린트 스테이션 (행사 직전)
- `print_agent.py` = 기존 `print_server.py`의 `render_pdf/print_pdf` 재활용 + 큐 폴링(claim→render→print→complete). → 설계 §5
- 2대(PC+Chrome+SumatraPDF+기본프린터), `STATION_ID=A/B`, `service_role`은 그 PC 환경변수에만.
- `live/print_admin.html` = 큐 모니터/재인쇄/스테이션 상태. → §6

---

## 체크리스트
- [ ] Vercel 배포 + URL 확보 + 실기기(스탠바이미) 테스트
- [ ] Supabase 스키마 RUN + 관리자 계정 + 키 확보
- [ ] supa.js 키 입력
- [ ] (조율 후) 페이지 supa.js 포함 + curation_sync Supabase 이전 + 핀 마이그레이션
- [ ] 결과페이지 자동 enqueue + 출력번호
- [ ] print_agent.py 2대 + print_admin.html
- [ ] JSON 슬림화
- [ ] 리허설(설문→인쇄 풀사이클)

## 보안 한 줄
**service_role 키는 스테이션 PC 환경변수에만.** 절대 깃·프런트·supa.js 금지.
