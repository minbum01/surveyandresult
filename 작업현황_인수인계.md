# 합격수기 탐색 시스템 — 작업현황 인수인계

> 마지막 작업일: 2026-06-19  
> 깃 레포: https://github.com/minbum01/surveyandresult  
> 로컬 서버: `python -m http.server 8181` → http://localhost:8181/live/home.html

---

## 프로젝트 구조 한눈에 보기

```
설명회/
├── live/
│   ├── home.html              ← 시험 선택 랜딩 (경찰/공무원/소방 카드)
│   ├── index.html             ← 공무원 설문+결과 (구조 다름, 하드코딩 방식)
│   ├── police_survey.html     ← 경찰 설문+결과 (JSON 태그 매칭 방식) ← 오늘 작업
│   ├── fire_survey.html       ← 소방 설문+결과 (보류 결정)
│   └── images/                ← 질문 배경 이미지
├── police_reviews_data.json   ← 경찰 후기 DB (1,337개)
├── fire_reviews_data.json     ← 소방 후기 DB (396개)
├── live/reviews_data.json     ← 공무원 후기 DB (2,114개)
├── 경찰_설문_태그_매칭_보고서.md  ← 태그 분석 상세 보고서
└── 작업현황_인수인계.md         ← 이 파일
```

---

## 완료된 작업 ✅

| 날짜 | 작업 내용 |
|------|----------|
| 이전 | 공무원 설문 `index.html` (8문항, 결과 페이지 완성) |
| 이전 | 경찰·소방 후기 크롤링 + 태그 추출 완료 |
| 이전 | `police_reviews_data.json`, `fire_reviews_data.json` 생성 |
| 오늘 | `home.html` 시험 선택 랜딩 신규 생성 |
| 오늘 | `police_survey.html` 경찰 설문 신규 생성 (7문항, 태그 매칭) |
| 오늘 | `fire_survey.html` 소방 설문 신규 생성 (보류) |
| 오늘 | 경찰 Q3 전형 2택→4택 (공채/간부/101경비단/경채) |
| 오늘 | 경찰 Q4 준비기간 2택→4택 (6개월미만~2년이상) |
| 오늘 | 경찰 TAG_EXP 전면 수정 (DB 태그명과 불일치 해소) |
| 오늘 | 통계 바 실제 비율 반영 (공채 94% / 성별 87:13) |
| 오늘 | h-match-cnt ID 참조 버그 수정 |
| 오늘 | 태그·매칭 종합 보고서 작성 |

---

## 바로 해야 할 작업 🔴 (우선순위 순)

### 1. 경찰 TAG_EXP 버그 3개 수정 (`live/police_survey.html`)

**[1-A] `독학위주` 태그 의미 왜곡 수정**
```javascript
// 현재 (잘못됨)
'독학위주': ['독학탈피', '단권화'],
// 독학탈피 = "독학을 그만뒀다" → 반대 의미!

// 수정안
'독학위주': ['단권화', 'OX학습', '기출중시'],
```

**[1-B] 영어 TAG 0건 항목 제거**
```javascript
// 현재 (0건 태그 포함)
'영어약자': ['단어암기', '이해+암기병행', '자투리시간활용', '영어약점'],
// 단어암기(0건), 영어약점(0건) → DB에 없음

// 수정안
'영어약자': ['이해+암기병행', '자투리시간활용', '시간관리'],
```

**[1-C] 경찰간부 선택 시 결과 페이지 안내 추가**
- 경찰간부 전형 후기가 DB에 **1건뿐** → 매칭이 사실상 안 됨
- `renderResult()` 함수 내 examType === '경찰간부' 조건으로 경고 문구 출력 필요
- 위치: `// HERO` 블록 아래 또는 NOTES 섹션 위

---

### 2. 공무원 `index.html`에 홈 링크 추가 (`live/index.html`)

현재 공무원 페이지에는 `home.html`로 돌아가는 버튼이 없음.  
경찰 페이지처럼 좌상단에 `← 시험 선택` 버튼 추가 필요.

**인트로 화면 헤더에 추가할 코드**:
```html
<a href="home.html" class="border border-white/15 rounded-full px-4 py-1.5 
  text-[10px] font-bold tracking-widest uppercase text-white/50 
  hover:text-white/80 transition-colors">
  ← 시험 선택
</a>
```

**결과 화면 상단 바에도 추가**:
```html
<a href="home.html" class="flex items-center gap-2 text-xs font-bold 
  text-zinc-500 hover:text-black px-4 py-2 bg-white rounded-full 
  shadow-sm active:scale-95 transition-colors">
  ← 시험 선택
</a>
```

---

### 3. Q1 상황 질문 개선 검토 (선택사항) (`live/police_survey.html`)

현재 Q1(상황)은 태그 매칭에 기여 없음 (전업/재학/퇴직 → DB 0건).  
아래 두 가지 방향 중 하나 선택:

**옵션 A: Q1을 수험 경험 질문으로 교체** (권장)
```
처음 도전인가요?  →  태그: 초시생(246건), 노베이스(109건)
재도전 중인가요?  →  태그: 재시생(130건), 재도전(141건)
```
→ TAG_EXP도 같이 교체 필요

**옵션 B: Q1 유지 (페르소나 UI용으로만 사용)**
→ 현재 상태 유지. 매칭 점수 기여는 포기.

---

## 중기 작업 🟡 (다음 세션)

### 4. 공무원 설문 TAG 매칭 방식 통일 (큰 작업)

- 현재 `index.html`은 **하드코딩된 PERSONAS/STATS/RADAR_SCORES**로 동작
- 경찰처럼 실제 `reviews_data.json` (2,114개) 에서 태그로 매칭하는 방식으로 교체 필요
- 작업량: 하루 이상 예상
- 우선 경찰 페이지가 완성된 뒤 착수 권장

### 5. 모의고사 활용 선택지 추가 검토 (`live/police_survey.html`)

보고서에서 발견: `동형모의고사(767건)`, `OX학습(484건)` 태그가 DB에 고밀도로 존재하나 현재 Q6에 없음.
- Q6 공부방식에 "모의고사 집중" 선택지 추가 → `['동형모의고사','OX학습','진도별문제풀이활용']`

---

## 페이지별 현재 완성도

| 페이지 | 완성도 | 설명 |
|--------|--------|------|
| `home.html` | ✅ 100% | 시험 선택 랜딩, 완성 |
| `police_survey.html` | 🟡 85% | 태그 버그 3개 미수정 |
| `index.html` (공무원) | 🟡 80% | 홈 링크 없음, 하드코딩 방식 |
| `fire_survey.html` | ⏸ 보류 | 소방은 현재 작업 제외 결정 |

---

## 데이터 현황

| 시험 | 후기 수 | DB 파일 | 상태 |
|------|---------|---------|------|
| 공무원 | 2,114개 | `live/reviews_data.json` | ✅ 완료 |
| 경찰 | 1,337개 | `police_reviews_data.json` | ✅ 완료 |
| 소방 | 396개 | `fire_reviews_data.json` | ✅ 완료 (페이지 보류) |

---

## 집에서 이어서 시작하는 법

```bash
# 1. 레포 클론 (처음) 또는 pull (이미 있으면)
git clone https://github.com/minbum01/surveyandresult.git
cd surveyandresult

# 2. 로컬 서버 실행
python -m http.server 8181

# 3. 브라우저에서 확인
# http://localhost:8181/live/home.html

# 4. 바로 할 작업: police_survey.html 열고 TAG_EXP 수정
# - 독학위주 수정
# - 영어약자 수정
# - 경찰간부 결과 안내 추가
```

---

## 참고 파일

| 파일 | 용도 |
|------|------|
| `경찰_설문_태그_매칭_보고서.md` | 전체 태그 DB 히트 수, 문제점, 개선안 상세 정리 |
| `live/police_survey.html` | 경찰 설문 소스 (TAG_EXP: 1250번째 줄 근처) |
| `live/index.html` | 공무원 설문 소스 (2,017줄) |
