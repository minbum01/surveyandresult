# 인수인계 — 경찰 후기 작업 (완전 처음부터)

> 작성: 서버/배포 터미널 · 2026-06-26
> 경찰은 큐레이션이 **아예 시작 안 됨**. 공무원과 질문·데이터가 다름. 아래 순서대로.

---

## 0. 미션
경찰 결과페이지(`live/police_result.html`)를 **모든 답변 조합에서 좋은 후기가 뜨도록** 큐레이션 + 완성. 공무원에서 한 것(자동 상위N 선정 → 사람 검수 → 랜덤 노출)을 경찰에도 적용.

---

## 1. 시작 전 알아둘 것

### 1-1. 큐레이션 저장소 (코드 분리 불필요 — 파일로 분리)
- `curation_sync.js`는 단일 키 `admin_curation_v2` 사용 → 공무원·경찰이 같은 localStorage를 씀.
- **하지만 코드 고칠 필요 없음.** 동시에 한 브라우저에서 두 시험 안 하면 충돌 없음:
  - 공무원 핀 끝 → **내보내기 `공무원_curation.json`**
  - 경찰 핀(따로) → **내보내기 `경찰_curation.json`**
  - **배포 때 Supabase `curation` 테이블이 시험별 행(exam)으로 분리** (설계 §3 이미 그렇게 됨).
- 즉 **각 시험 큐레이션 = 별도 파일**로 관리하면 끝.

### 1-2. 경찰 전용 관리자 큐레이션 페이지 = 새 HTML 만들면 됨
- `no01_폴백_매칭수.html`은 **공무원 전용**(Q·태그·매트릭스 공무원 기준).
- → **no01을 복제해 경찰 전용 `police_admin.html`(가칭) 제작.** 바꿀 것:
  - Q 구조: 경찰 7문항(상황·성별·전형·기간·시간·방식…)으로 (police_survey.html 참조)
  - TAG_EXPANSION: 경찰용(police_result.html 385행대의 경찰 태그맵)
  - 매트릭스/조합 축: **상황 × 전형** 등 경찰 기준
- **저장소 충돌 없음(이미 처리됨)**: `curation_sync.js`가 `window.CURRENT_EXAM`으로 키 자동 분리.
  - 공무원 = `admin_curation_v2` (그대로) / 경찰 = `admin_curation_v2_경찰`
  - `police_result.html`엔 `window.CURRENT_EXAM='경찰'` **이미 추가됨**.
  - 새 `police_admin.html`도 **상단에 `window.CURRENT_EXAM='경찰'` 설정**하고, 저장/읽기 키를 `admin_curation_v2_경찰`로 쓰면 됨.
  - → 공무원 핀과 **완전 분리**, 동시에 떠 있어도 안 섞임.

---

## 2. 경찰 설문 구조 (공무원과 다름!)
`live/police_survey.html` — **7문항**:
| Q | 항목 | 선택지 |
|---|---|---|
| Q1 | 상황 | 전업/직장병행/재학/육아병행/퇴직/아르바이트병행 (공무원과 동일) |
| Q2 | 성별 | 남성 / 여성 |
| Q3 | 전형 | 일반공채 / 경찰간부 / 101경비단 / 경채 |
| Q4 | 준비기간 | 6개월미만 / 6개월~1년 / 1년~2년 / 2년이상 |
| Q5 | 공부시간 | 5시간미만 / 5~8 / 8~10 / 10시간이상 |
| Q6 | 공부방식(복수) | 인강/기출/회독/독학/스터디 |
| Q7 | (고민/약점 등) | police_survey.html 확인 |
> 공무원에 있던 **직렬·베이스(Q3·Q4)가 없고, 성별·전형**이 있음 → 큐레이션 조합 축이 다름.
> ⚠️ 경찰간부 전형은 후기 DB에 **극소수** → 매칭 거의 안 됨(공무원 보고서 참고). 결과페이지에 안내 문구 권장.

---

## 3. 경찰 데이터 / 결과페이지 현황
- `police_reviews_data.json` (루트) — **1,337건**. 필드: id,title,url,exam,grade,region,job,period,year,category,tags,inst …
  - ⚠️ 공무원과 달리 **passage/quotes 필드 유무 확인 필요** (renderNo06~10이 passage/quotes 쓰면 경찰 데이터에 그 필드 있어야 함). 없으면 build 스크립트로 추출 필요.
- `live/police_result.html` — 이미 `curation_sync.js` + `renderNo06~10` + `applyPool` 연결됨, `fetch('../police_reviews_data.json')`.
- 경찰 태그확장맵은 police_result.html 안에 별도(공무원과 다른 태그) — 385행대.

---

## 4. 작업 순서
1. **(서버터미널) 큐레이션 exam-scoping** (§1-1) — 최우선.
2. **경찰 관리자 큐레이션 페이지** 마련 (§1-2).
3. **경찰 auto_curate**: `auto_curate.py`를 경찰용으로 복제(`auto_curate_police.py`):
   - 입력 `police_reviews_data.json`, 경찰 TAG_EXPANSION(police_result.html에서 복제)
   - 조합 축 = **상황(Q1) × 전형(Q3)** 정도(직렬 없음). 성별(Q2)은 풀 안에서 자동 우선매칭 권장.
   - 칸 성격별 점수(공무원과 동일: no.01 감정형 / no.06 실전형).
   - 출력 → 경찰 관리자에 import → 사람 검수.
4. **검수**(사람): 조합별 자동 상위N에서 나쁜 것 제거.
5. no.12~14 등 정적 영역은 공무원과 동일 이슈 → `결과페이지_항목정리.md` 방식 참고.

---

## 5. 과목 키워드 (과목별 엮기용)

### 경찰 과목 (2022 개편 기준: 헌법·형사법·경찰학 / 영어·한국사는 검정제)
| 과목 | 키워드(동의어 묶음) |
|---|---|
| 헌법 | 헌법, 기본권, 헌재, 위헌, 헌법재판 |
| 형사법 | 형사법, 형법, 형소법, 형사소송법, 수사, 판례(형사) |
| 경찰학 | 경찰학, 경찰행정, 경찰행정법, 경무, 생활안전, 수사경찰 |
| 영어 | 영어, 단어, 어휘, 독해, 문법(영어), 영단어, 토익 |
| 한국사 | 한국사, 한능검, 근현대사, 사료 |
| 범죄학(경채) | 범죄학, 범죄, 형사정책 |
| 체력 | 체력, 체력시험, 윗몸, 악력, 50m, 1000m, 운동 |
| 면접 | 면접, 집단면접, 인성검사, 면접스터디 |

### 공무원 과목 (참고 — 같은 작업 시)
| 과목 | 키워드 |
|---|---|
| 국어 | 국어, 문법, 비문학, 문학, 어휘, 독해 |
| 영어 | 영어, 토익, 단어, 어휘, 문법, 독해, 구문, 영단어 |
| 한국사 | 한국사, 한능검, 근현대사, 사료 |
| 행정법 | 행정법, 행정쟁송, 판례, 행쟁, 행정구제 |
| 행정학 | 행정학, 정책학, 조직론, 인사행정 |
| 헌법 | 헌법, 기본권, 헌재 |
| 세법 | 세법, 국세, 부가가치세, 소득세, 법인세 |
| 회계학 | 회계, 회계학, 원가, 재무회계 |
| 교육학 | 교육학, 교육과정, 교육심리 |
| 사회 | 사회, 법과정치, 경제, 사회문화 |
| 과학 | 과학, 물리, 화학, 생물, 지구과학 |
| 수학 | 수학 |

> "안 엮인 게 많다" = 위 키워드로 후기/강사/교재를 과목에 매핑. 동의어 누락분은 검수하며 추가.

---

## 6. 경계선 (공무원 작업 터미널과 안 겹치게)
- `result.html`(공무원)·`build_result_stats.py` = 공무원 결과페이지 터미널 소유 → 건드리지 말 것.
- `curation_sync.js`·exam-scoping·배포 = 서버터미널.
- 경찰 전용 파일(`police_result.html` 수정, `auto_curate_police.py`, `police_admin.html`)이 경찰 작업 영역.
- **이 폴더는 여러 터미널 공유** → git `add -A` 금지(남의 미커밋 휩쓸림). 자기 파일만 add. push 전 `git pull`.
