# 서버/배포 인수인계 (집에서 이어하기용) — 2026-06-27

> 서버/배포 터미널이 한 일 전부 + 상태 + 이어가는 법. **집에서도 GitHub 레포만 pull 하면 그대로 이어집니다** (핀·큐레이션은 클라우드=Supabase에 있어 기기 무관).

---

## 0. 한 줄 요약
**라이브 사이트 + Supabase(큐레이션·인쇄큐) + 클라우드 인쇄 배선까지 다 만들어 배포 완료.** 남은 건 ①경찰 결과 enqueue(경찰 터미널) ②행사 직전 프린트대 PC 셋업.

---

## 1. 접속 정보 (공개 OK인 것만)
| 항목 | 값 |
|---|---|
| 라이브 사이트 | https://surveyandresult.vercel.app/ |
| 인쇄 모니터 | https://surveyandresult.vercel.app/live/print_admin.html |
| GitHub | https://github.com/minbum01/surveyandresult (branch: main) |
| Supabase URL | https://zxjhgcumweqaxcemoiga.supabase.co |
| anon 키 | `live/supa.js` 안에 있음 (공개 OK) |
| 관리자 계정 | minbum01@gmail.com (비번은 본인 보관) |
| 🔴 service_role 키 / DB 비번 | **레포·문서 금지.** 프린트대 PC에만. |

- **배포 방식**: GitHub `main`에 push → **Vercel 자동 재배포**. 따로 클릭 없음.

---

## 2. ✅ 완료한 것 (전부 커밋·배포됨)
1. **Vercel 배포** — 라이브, 전 경로 200.
2. **Supabase 연동** — `supa.js`에 URL·anon. 테이블 `curation`/`print_jobs`/`stations` + RPC 생성됨.
3. **큐레이션 ↔ 서버** — 결과페이지가 서버 핀 읽음 / `no01`의 ☁ 서버 저장으로 업로드.
   - **공무원 핀 = Supabase에 올라가 있음** (`curation` exam='공무원', no.01 약 231핀). 라이브 반영됨.
4. **Q4 병합** (커밋 1d7c457) — `노베이스`+`수능베이스`→"노베이스 / 수능 경험" 통일, `고용노동직`을 기타직렬→행정직 이동. 설문4종+result+no01, 라이브 반영.
5. **버그 수정** — `curation_sync.js`: 빈 서버가 로컬 핀을 가리던 문제(서버 행 없으면 localStorage 폴백) + `?local=1` 강제로컬.
6. **클라우드 인쇄 배선** (커밋 afa8791) — 아래 3번.

---

## 3. 🖨️ 인쇄 시스템 (자세히는 `인쇄시스템_할일.md`)
**흐름**: 태블릿 설문 → "리포트 출력" → Supabase 인쇄큐 등록(+출력번호) → 프린트대 PC의 `print_agent.py`가 받아 잉크젯 2대 출력 → 종이 우상단 #번호. 안 되면 모니터에서 재인쇄.

- **결과페이지**: 출력버튼 = `window.enqueuePrint()` (supa.js). 화면·종이에 출력번호.
- **모니터** `live/print_admin.html`(신규): 큐 실시간·어느 태블릿·상태·재인쇄·스테이션 heartbeat. 로그인 필요.
- **에이전트** `print_agent.py`: `PRINTER` 환경변수로 프린터 지정. 2대 = 인스턴스 2개(STATION_ID=A/B, PRINTER 각각). 큐가 자동 분배.
- **device 추적**: 태블릿을 `...?d=탭A`로 1회 열면 식별 저장 → 모니터에 표시.
- **마이그레이션 적용됨**: `print_jobs.device` 컬럼 + `enqueue_print` 4인자 (검증: 출력번호 반환 OK). 테스트행 #1 큐에 있음(무해).

### 행사 직전 남은 셋업 (프린트대 PC)
- Chrome + `tools/SumatraPDF.exe` + 잉크젯 2대(프린터 이름 확인) + service_role 키.
- 에이전트 2창 실행 (명령은 `인쇄시스템_할일.md` §3-2).
- 태블릿마다 `?d=탭A/B…` 1회.

---

## 4. ⏳ 남은 일 (담당)
| 일 | 담당 | 비고 |
|---|---|---|
| 경찰 결과 "출력" → enqueue 연결 | **경찰 터미널** | 공무원과 동일 패턴. 명세 = `터미널간공유문서.md` |
| 경찰 결과 영역 '답변 연결성' 개선(no.07 등) | 경찰 터미널 | 사용자 지적건 |
| 경찰 큐레이션 → Supabase(exam='경찰') | 나(서버) | 경찰 검수 끝나면 police_admin ☁ → 내가 검증 |
| 프린트대 PC 셋업 + 에이전트 실행 | 사람 | 행사 직전 |
| 공무원 핀 추가 검수 | 사람 | no01/curate_confirm → ☁ 서버 저장 |

---

## 5. 🏠 집에서 이어하는 법
1. **레포 pull**: `git pull` (집 PC에 클론돼 있으면) — 모든 코드 최신.
2. **핀/큐레이션은 서버에 있음** → 집 브라우저에서 `no01_폴백_매칭수.html` 열고 **⬇ 서버 불러오기** 하면 office에서 올린 231핀이 그대로 내려옴 → 이어서 검수 → ☁ 서버 저장.
   - ⚠️ 핀 수정 후 라이브 반영은 **항상 ☁ 서버 저장** 1번. (localStorage만으론 라이브 안 바뀜)
3. **로컬 서버**: `python -m http.server 8181` (또는 `python print_server.py`) → `localhost:8181/live/...`
4. **배포**: 코드 고치면 `git add <내파일> && git commit && git push` → Vercel 자동 재배포.

---

## 6. ⚠️ 주의 (여러 터미널 공유 폴더)
- `git add -A` 금지 — **내 파일만** add. push 전 `git pull`.
- 경찰 파일(`police_*`)·공무원 result/설문은 다른 터미널 소유 — 조율은 `터미널간공유문서.md`.
- service_role 키·DB 비번 = 레포/프런트 절대 금지(프린트대 PC만).

---

## 7. 핵심 파일
| 파일 | 역할 |
|---|---|
| `live/supa.js` | Supabase 클라이언트 + device + enqueuePrint |
| `live/curation_sync.js` | 큐레이션 엔진(서버/로컬 핀) |
| `live/print_admin.html` | 인쇄 모니터(신규) |
| `print_agent.py` | 프린트대 에이전트 |
| `supabase/schema.sql` / `migration_print_device.sql` | DB 스키마 |
| `인쇄시스템_할일.md` | 인쇄 셋업 상세 체크리스트 |
| `터미널간공유문서.md` / `3터미널에게공지.md` | 터미널 간 조율 |
| `현재상태_요약.md` | 전체 프로젝트 재시작 요약 |

## 8. 최근 커밋
- `afa8791` 클라우드 인쇄 큐 배선(enqueue+모니터+출력번호)
- `1d7c457` Q4 병합 + 고용노동직 + 빈서버 폴백 수정
